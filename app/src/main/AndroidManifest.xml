<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.akhil.akhildixit.offlineatten">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".activities.otheractivities.MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

          </activity>
        <activity android:name=".activities.branchandsection.AddBatch">

        </activity>
        <activity android:name=".activities.studentdetails.AddStudent"></activity>
        <activity android:name=".activities.studentdetails.ViewStudent">

        </activity>
        <activity android:name=".activities.attendance.takeAttendance"></activity>
        <activity android:name=".activities.attendance.quickatten.TakeQuickAttendance"></activity>
        <activity android:name=".activities.attendance.ViewAttendance"></activity>
        <activity android:name=".activities.otheractivities.Help"></activity>
    </application>

</manifest>